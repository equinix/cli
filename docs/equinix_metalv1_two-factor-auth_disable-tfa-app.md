## equinix metalv1 two-factor-auth disable-tfa-app

Disable two factor authentication

### Synopsis

Disables two factor authentication.

Use --request flag to provide optional JSON payload fields.

```
equinix metalv1 two-factor-auth disable-tfa-app [flags]
```

### Options

```
  -h, --help                 help for disable-tfa-app
      --request string       JSON payload for additional optional fields not exposed as flags
      --x-otp-token string   x-otp-token field
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.config/equinix/equinix.yaml)
      --debug           Enable debug logging for HTTP requests
  -f, --format string   Format to use for output (json or yaml) (default "json")
```

### SEE ALSO

* [equinix metalv1 two-factor-auth](equinix_metalv1_two-factor-auth.md)	 - Manage two-factor-auth resources

